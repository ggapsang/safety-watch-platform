# ── 웹 ──────────────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# ── DB ──────────────────────────────────────────────────────────────────
SQLAlchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.14
# 개발 시 SQLite 로 띄우고 싶을 때만 필요 (DATABASE_URL=sqlite+aiosqlite:///./dev.db)
aiosqlite>=0.20

# ── MQTT (카메라 ONVIF 이벤트 수신) ─────────────────────────────────────
aiomqtt>=2.3

# ── 영상 (RTSP 수신 → MJPEG 재송출) ─────────────────────────────────────
opencv-python-headless>=4.10
numpy>=1.26,<3

# ── 기타 ────────────────────────────────────────────────────────────────
# 옛 암호문을 푸는 마이그레이션(3864ed7c06b2)에만 쓴다. 비밀번호는 이제 평문으로 둔다.
# 그 마이그레이션이 모든 배포에서 지나가고 나면 지워도 된다.
cryptography>=43
httpx>=0.27             # 카메라 연결 테스트(HTTP/ONVIF 프로브)
# Windows 에는 IANA 시간대 DB 가 없다. 통계는 현장 로컬 시간대로 끊으므로 필수.
tzdata>=2024.2 ; sys_platform == "win32"
