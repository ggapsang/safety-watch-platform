# 빌드 컨텍스트에서 제외 — 이미지 크기와 빌드 시간에 직접 영향을 줍니다.
**/node_modules
**/dist
**/__pycache__
**/*.pyc

# 로컬 개발용 가상환경(수백 MB) — 이게 들어가면 이미지가 통째로 부풀어 오릅니다.
**/.venv
**/venv

# 자격증명·런타임 데이터
.env
server/data
server/*.db
server/static

# 모델 바이너리는 볼륨으로 마운트합니다(이미지에 굽지 않습니다).
models/*
!models/README.md
