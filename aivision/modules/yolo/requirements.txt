# 추론
numpy>=1.26,<3
opencv-python-headless>=4.10
# GPU 판.
#
# **[cuda,cudnn] 을 빼면 안 된다.** onnxruntime-gpu 휠 자체에는 CUDA 라이브러리가 들어
# 있지 않다. 그냥 설치하면 provider 는 목록에 보이는데 여는 순간
# 'libcublasLt.so.13: cannot open shared object file' 로 실패하고 조용히 CPU 로
# 내려간다 — 로그를 안 보면 GPU 를 쓰는 줄 안다. 실제로 그랬다.
#
# 호스트에는 **NVIDIA 드라이버만** 있으면 된다. CUDA Toolkit·cuDNN 을 시스템에 깔 필요는
# 없다 — 이 extras 가 pip 패키지로 받아 온다. 그만큼 이미지가 커진다(1.3GB -> 몇 GB).
#
# GPU 가 없는 현장이라면 이 줄을 onnxruntime 으로 되돌리고 compose 의 deploy 블록을
# 주석 처리한다. 카메라 여섯 대를 CPU 로 돌렸더니 설정한 3fps 가 실제로는 0.2fps 였고
# (코어 열 개를 다 쓰고도), 그것이 GPU 로 옮긴 이유다.
onnxruntime-gpu[cuda,cudnn]>=1.18
paho-mqtt>=2.1

# 모듈 화면·API
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9
# 모델 파일 업로드(multipart)를 받으려면 필요하다.
python-multipart>=0.0.9

# 학습 의존성(torch·scipy·pandas·matplotlib·seaborn·tensorboard·onnx-simplifier 등)은
# 걷어냈다. 학습은 이 모듈의 일이 아니다 — refs/yolov7-training/ 을 보라.
# 런타임은 onnxruntime 하나이고, 그래서 이미지가 GB 단위로 줄었다.
